views:
  telnetConnected:
    ansi: connected
    hideCursor: true
    clearScreen: true
    next:
      view: authWelcome
      delay: 1000

  sshConnected:
    ansi: connected
    hideCursor: true
    clearScreen: true
    next:
      view: authWelcome
      delay: 1000
#    next: authWelcome
#    next:
#    - view: authSshSecondFactor # require 2FA
#      acs: AR2
#    - view: demo # this drops a user into the demo view
#      acs: GM[demo]
#    - view: authWelcome

  authWelcome:
    ansi: _welcome
    hideCursor: true
    clearScreen: true
    next: interstitial
    prompt: pause

  interstitial:
    ansi: testing
    clearScreen: true
    prompt: pause


prompts:
  pause:
    ansi: _pause
    lineFeed: no